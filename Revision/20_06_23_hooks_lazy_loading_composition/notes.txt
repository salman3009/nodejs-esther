Performance Improvement:
1.useRef:
//disabled,value,focus --- useRef to avoid interacting with real DOM.
Lazy loading 
To integrate routing:
1.react-router-dom
//To load the component later once we hit the particular url in the browser.
React suspense
//Whenever loading is happening for lazy loading component it helps to provide spinner
API call
//third party package called axios for api call
//It support all methods like get,post,patch,delete 
//It will take care of parsing the data from json to javascript and javascript to json 
//It will support other concepts like common headers and interceptors.
interceptor
 //It will act like middleware between frontend and backend 
 //It helps to attach token in headers for all the api call 
React composition 
//

class component 
life cycle in class component 
error boundary 